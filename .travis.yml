language: java

sudo: false

services:
  - docker

jdk:
  - oraclejdk8

addons:
  apt:
    packages:
      - rpm

cache:
  directories:
    - $HOME/.m2/repository

script:
  - mvn test -P ci -B javadoc:javadoc
